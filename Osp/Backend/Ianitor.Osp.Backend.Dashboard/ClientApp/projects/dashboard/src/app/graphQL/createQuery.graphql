mutation createQuery($entities: [SystemQueryInput]!) {
  createSystemQuerys(entities: $entities) {
    rtId
    name
    queryCkId
    columns
    sorting
    fieldFilter
    attributeSearchFilter
    textSearchFilter
  }
}
