query getCkAttributeDetails($attributeId: String) {
  constructionKitAttributes(attributeId: $attributeId) {
    items {
      attributeId

      attributeValueType
      scopeId
      selectionValues {
        key
        name
      }
      defaultValues
      defaultValue
    }
  }
}
