query getServiceHookDetails($rtId: OspObjectIdType!) {
  systemServiceHookConnection(rtId: $rtId) {
    totalCount

    items {
      rtId
      enabled
      name
      queryCkId
      serviceHookAction
      serviceHookBaseUri
      fieldFilter
    }
  }
}
